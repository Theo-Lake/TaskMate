import { View, Text, StyleSheet } from "react-native";
import { styles } from "./styles";

export const NavBar = () => {
  return (
    <View>
      <Text style={styles.container}>NavBar</Text>
    </View>
  );
};
